from distutils.core import setup

setup(
    name="cleaner",
    version="0.0",
    description="a simple tool that cleans up your directories recursively, by file extension",
    author="Eric Schles",
    packages=["cleaner"]
)
